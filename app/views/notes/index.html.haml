
.wrapper_with_padding
	#notes.clearfix
		-unless @notes.blank?
			- @notes.each do |note|
				%a{href:(url_for [note])}
					.note
						%p.title= note.title
						%p.time= time_ago_in_words note.created_at
		-else
			%h2 Add a note
			%p It appears you haven't created any notes yet... Let's fix that,why don't you add a new note down below
			%button= link_to "Lets Do it", new_note_path
	